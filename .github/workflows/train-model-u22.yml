name: Train Model u22

on:
  push:
    branches:
      - main

jobs:
  train-model:
    runs-on: ubuntu-22.04-arm-os # Custom ARM64 runner
    container:
      image: armswdev/pytorch-arm-neoverse:r24.07-torch-2.3.0-openblas
      options: --user 1001
    name: Train the Model
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: pip install torch==2.4.0 torchvision==0.15.0
      - name: Run training script
        run: python scripts/train_test_compare.py
    # approach
    #  - name: Run the build process with Docker
    #    uses: addnab/docker-run-action@v3
    #    with:
    #      image: armswdev/pytorch-arm-neoverse:r24.07-torch-2.3.0-openblas
    #      options: -v ${{ github.workspace }}:/home/ubuntu
    #      run: |
    #        python ./scripts/train_model.py
